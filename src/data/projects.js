import { DataScience, PostgresqlIcon, NodejsIcon, Python, Flask, Angular, MaterialUi, ReactIcon, FirebaseIcon, Windows } from '../components/SvgIcons';
import widsThumbnail from '../assets/wids_2023_datathon.webp';
import caribbeanAsrThumbnail from '../assets/zindi_caribbean_voices_hackathon.webp';
import pokemonDashboardProPic from '../assets/pokemon_dashboard_pro_pic.webp';
import chimpShot from '../assets/screenshots/chimp-test-game.webp';
import ideaSprintShot from '../assets/screenshots/idea-sprint.webp';
import movieShot from '../assets/screenshots/movie-app.webp';
import uwiScraperShot from '../assets/screenshots/uwi-scraper.webp';
import pythonOcrShot from '../assets/screenshots/python-ocr.webp';
import biReportingShot from '../assets/screenshots/bi-automatic-reporting.webp';
import fastapiMockerShot from '../assets/screenshots/fastapimocker.webp';

export const projects = [
  {
    id: 'chimp-test-game',
    highlight: 'Two cognitive memory games sharing a Supabase-backed live leaderboard, with grid-rotation mechanics and three configurable difficulty modes.',
    primaryTech: ['React 19', 'TypeScript', 'Supabase', 'Tailwind CSS v4'],
    screenshot: chimpShot,
    title: 'Chimp Test & Visual Memory Game Suite',
    shortDescription: 'A dual-game cognitive memory training suite featuring the classic Chimp Test (working memory) and Visual Memory Test (pattern recall with grid rotation). Built with React 19, TypeScript, Tailwind CSS v4, shadcn/ui, and Supabase-powered real-time leaderboards.',
    category: 'Full Stack',
    technologies: ['React 19', 'TypeScript', 'Vite', 'Tailwind CSS v4', 'shadcn/ui', 'Framer Motion', 'Supabase', 'PostgreSQL', 'Lucide React'],
    thumbnail: null,
    featured: true,
    githubUrl: 'https://github.com/firepenguindisopanda/game-test-chimp',
    liveUrl: 'https://game-test-chimp.vercel.app/',
    markdown: '/markdowns/chimp-test-game.md',
    techIcons: [
      { Icon: ReactIcon, label: 'React' },
      { Icon: PostgresqlIcon, label: 'PostgreSQL' },
    ],
    tasks: [
      'Classic Chimp Test: Memorize and click numbered tiles in ascending order across increasing difficulty levels (4 to 15+ tiles)',
      'Visual Memory Test: Recall tile positions on a dynamically rotating grid with progressive grid sizes (3×3 to 7×7+)',
      'Three game modes (Beginner, Advanced, Super Advanced) with configurable view times (3s/5s/7s) for varied challenge',
      'Supabase-powered real-time global leaderboard with personal score tracking, mode filtering, and live score subscriptions',
      'Warcraft-themed audio feedback system with 100+ sound effects for tile clicks, level clears, wrong guesses, and game events',
      'Animated grid rotation mechanics in Visual Memory Test with stepwise 90° rotation and configurable rotation steps',
      'Dynamic difficulty scaling: tiles increase each level in Chimp Test, grid size grows every 2 levels in Visual Memory',
    ],
  },
  {
    id: 'idea-sprint',
    highlight: 'Eleven specialist agents turn a product idea into a full software spec through a phased LangGraph graph - with an adversarial skeptic, a user-contestable recommender, and deterministic cross-agent contradiction detection.',
    primaryTech: ['LangGraph', 'FastAPI', 'Next.js 16', 'Pinecone'],
    screenshot: ideaSprintShot,
    title: 'specs before code (IdeaSprinter): Multi-Agent Software Team',
    shortDescription: 'A multi-agent system where 11 specialist agents argue their way to a software specification. Built around making the reasoning legible and contestable: an adversarial skeptic, schema-forced verdicts when a user pushes back, and a deterministic validator that catches contradictions between agents.',
    description: 'A LangGraph StateGraph orchestrating 11 specialist agents (product owner, business analyst, solution architect, data architect, security analyst, UX, API designer, QA strategist, DevOps, technical writer, spec coordinator) plus 4 auxiliary agents, structured in three phases where the architecture and contract phases fan out concurrently via the Send API. The design bet is that generating a spec is easy and generating one a senior engineer will argue with productively is hard, so most of the system exists to make reasoning checkable: the Architecture Studio shows justifications and stated assumptions, runs a skeptic agent to produce the strongest case against its own recommendation, lets the user contest an assumption with the response schema-forced to separate "was the assumption wrong" from "does that change the recommendation" (collapsed into one field, the model withdrew recommendations the correction never undermined), and only then drafts an ADR that is not persisted until a human edits and saves it. Grounding comes from 10 role-scoped Pinecone corpora plus engineering books indexed in three sizes - nano as prompt guardrails, mini as judge rubrics, full for retrieval. Backed by real operational machinery: SLO error budgets, monthly token-cost enforcement with provider fallback, circuit breakers, Redis caching, and human approve/reject checkpoints between phases.',
    category: 'Full Stack',
    technologies: ['FastAPI', 'LangGraph', 'LangChain', 'Next.js 16', 'React 19', 'Pinecone', 'NVIDIA NIM', 'Python 3.12', 'TypeScript', 'Tailwind CSS v4', 'SQLAlchemy', 'Upstash Redis', 'NetworkX', 'Docker', 'Playwright'],
    thumbnail: null,
    featured: true,
    topPick: true,
    githubUrl: 'https://github.com/firepenguindisopanda/idea-sprinter',
    liveUrl: 'https://idea-sprinter-web.vercel.app/',
    markdown: '/markdowns/idea-sprint.md',
    evidence: 'A skeptic agent builds the strongest case against the system\'s own recommendation, and a deterministic NetworkX validator sweeps role-scoped sub-graphs for cross-agent contradictions before the spec is assembled.',
    techIcons: [ { Icon: DataScience, label: 'AI' }, { Icon: Python, label: 'Python' } ],
    tasks: [
      'LangGraph StateGraph in three phases - sequential foundation, then architecture and contract phases fanned out concurrently via the Send API - with no file-based IPC and no LLM calls outside graph nodes',
      '11 specialist agents whose personas live as markdown prompt files rather than string literals, so a role can be revised without touching Python',
      'Defensible architecture recommender built in four ordered steps: stated assumptions, an adversarial skeptic case against the recommendation, user contestation, then an ADR drafted from the reasoning trail',
      'Schema-forced contest verdicts separating "the assumption was wrong" from "that changes the recommendation" - collapsed into one field, the model conflated accepting a fact with changing a conclusion',
      'Deterministic ConsistencyValidator sweeping role-scoped knowledge-graph sub-graphs (NetworkX) for cross-agent contradictions before SRS assembly, with append-only writes',
      'QualityEngine scoring readability with a 100+ term domain whitelist, so multi-syllable jargon does not inflate the score past what structural complexity would give',
      'Persona-tailored intake for 4 user types with a sufficiency-based judge that asks whether there is enough to proceed rather than demanding technical perfection',
      'Two-layer RAG: 10 role-scoped Pinecone corpora plus engineering books indexed in nano/mini/full variants for guardrails, judge rubrics and retrieval respectively',
      'Operations rarely present in a portfolio project: SLO error budgets, per-request and monthly token-cost enforcement with provider fallback, circuit breakers, Redis-cached sessions and pattern library, audit trail',
      'Human-in-the-loop checkpoint records with explicit approve/reject endpoints at pipeline boundaries',
      'Next.js 16 / React 19 frontend with live per-agent progress, judge verdicts, Mermaid rendering in streamed markdown, and draft persistence across refreshes - 18 Vitest suites plus Playwright E2E',
    ],
  },
  {
    id: 'python-ocr',
    highlight: 'Receipts, handwritten lists and scanned answer sheets through one OCR core - with a diagnostics surface that shows why a reading is good or bad, and a mark-reading module that never mistakes a shaded bubble for a text problem.',
    primaryTech: ['FastAPI', 'OpenCV', 'Tesseract 5', 'React 19'],
    // Captured from the running app with the printed receipt sample read: the
    // word boxes tinted by confidence (note the amber one on "500ml") are the
    // diagnostics surface the write-up is about, so the screenshot argues the
    // same point the copy does.
    screenshot: pythonOcrShot,
    title: 'python_ocr: Document Extraction with a Diagnostics Surface',
    shortDescription: 'A modular monolith for document data extraction: three domain modules (receipts, grocery lists, answer sheets) on one shared OCR core, wrapped in a UI that draws every recognised word box over the page tinted by confidence.',
    description: 'Point a camera at a till receipt, a handwritten grocery list or a scanned multiple-choice quiz and get structured data back. Extraction is the easy half; the project is built around knowing whether the output is right. Three domain modules sit on a shared core under one rule - modules may import from core, never from each other - which is why adding the answer-sheet module cost one folder and one line. The core uses image_to_data rather than image_to_string, so every word carries a box and a score: the receipt parser finds prices by position rather than regex, the grocery parser clusters line positions into columns, and the UI can trace a suspect value back to the pixels it came from. The sharpest distinction in the project is that reading a shaded bubble is optical mark recognition, not text recognition - Tesseract returns nothing useful from one - so the OMR module measures pixel density with OpenCV, calibrates once against a blank form (a filled bubble merges with its own printed outline, so detection is least reliable exactly when it matters), and warps each scan onto the template via four corner registration marks.',
    category: 'Full Stack',
    technologies: ['Python 3.12', 'FastAPI', 'Tesseract 5', 'pytesseract', 'OpenCV', 'pypdfium2', 'React 19', 'TypeScript', 'Vite', 'Docker', 'pytest'],
    thumbnail: null,
    featured: true,
    topPick: true,
    githubUrl: null,
    liveUrl: null,
    markdown: '/markdowns/python-ocr.md',
    evidence: 'Arithmetic reconciliation catches the errors confidence cannot - a clean, confident 8 where the paper said 3 still fails if the line items do not sum to the printed subtotal.',
    techIcons: [
      { Icon: Python, label: 'Python' },
      { Icon: ReactIcon, label: 'React' },
    ],
    tasks: [
      'Modular monolith with one enforced rule - modules import from core, never from each other - proven by adding the OMR module as one folder plus one line in api.py, with receipt and grocery untouched',
      'Composable OpenCV preprocessing steps behind four named presets, plus a sweep endpoint that scores every preset against every page segmentation mode so choices are measured rather than guessed',
      'Shadow removal that estimates background illumination by dilate-and-median-blur, then divides it out - usually worth more on a photographed page than any other single step',
      'image_to_data throughout, so every word carries a bounding box and confidence: prices found by position, columns found by clustering, and a UI overlay tinted by confidence',
      'Arithmetic reconciliation on receipts, because Tesseract confidence catches illegible text but never a plausible misread',
      'Strikethrough detection by morphology - a wide horizontal opening over the middle band of a line box - since Tesseract has no concept of a crossed-out word',
      'OMR calibration against a blank form only, because on a marked sheet a filled bubble merges with its own printed outline',
      'Four corner registration marks warping each scan onto the template; a page skewed 1.4 degrees reads identically to a square one, asserted in test_skewed_page_reads_identically',
      'Four reported outcomes - marked, blank, multiple, ambiguous - never collapsed into a best guess, with confidence defined as the margin between the darkest bubble and the next darkest',
      'Handwriting handled by refusing it: the grocery module sets likely_handwritten and returns a warning at ~33% confidence rather than presenting noise as data',
      '101 tests in three layers - hand-built OcrDocument parser tests, endpoint tests asserting structure rather than exact OCR strings, and property tests asserting the claims the README makes',
      'Single-container deployment to Hugging Face Spaces via the Docker SDK, which is what allows apt-installing the Tesseract binary at all',
    ],
  },
  {
    id: 'fastapimocker',
    highlight: 'A hosted mock REST API with real persistence, real cookie auth with refresh rotation, and real per-IP rate limits - so a frontend meets its 401s, 429s and 503s in development rather than in production.',
    primaryTech: ['FastAPI', 'SQLModel', 'Neon', 'Upstash Redis'],
    // Captured from the live deployment, not a local run: the seeded row counts
    // and the "Live mock API" badge are the deployment reporting its own state,
    // which is the most useful thing a thumbnail can show for a hosted service.
    screenshot: fastapiMockerShot,
    title: 'FastAPIMocker: A Mock API That Behaves Like a Real One',
    shortDescription: 'A hosted mock REST API for frontend development. Most mocks are read-only fictions where POST politely pretends to work; this one persists writes, rotates refresh tokens, rate-limits per IP across instances, and models dependency failure explicitly.',
    description: 'A mock API\'s job is not to return plausible JSON - it is to be the environment your client code will actually run in, minus the consequences. That reframing decides the whole design: the limiter is real and its headers are CORS-exposed so a browser can read them, auth is cookie-based JWT with rotating refresh tokens and replay rejection, writes persist, and dependency failure is modelled rather than hidden. Both third-party dependencies sit behind circuit breakers with deliberately opposite judgements - Postgres is load-bearing so it fails closed with 503 plus Retry-After, while the rate limiter fails open to in-memory counters, because an unavailable guardrail must never be more damaging than the abuse it prevents. Neon\'s libpq connection string is normalised for asyncpg at the boundary, preserving libpq\'s semantics for sslmode=require (encrypt, not verify) and detecting the -pooler endpoint so asyncpg\'s prepared statements do not collide behind PgBouncer. Seeds roughly 9,000 rows on first boot under a Postgres advisory lock so parallel instances cannot double-seed.',
    category: 'Full Stack',
    technologies: ['Python 3.13', 'FastAPI', 'SQLModel', 'SQLAlchemy', 'Alembic', 'Neon Postgres', 'Upstash Redis', 'asyncpg', 'pydantic-settings', 'JWT', 'pytest', 'uv'],
    thumbnail: null,
    featured: true,
    topPick: true,
    githubUrl: null,
    liveUrl: 'https://fastapimocker.fastapicloud.dev/',
    markdown: '/markdowns/fastapimocker.md',
    evidence: '213 tests that never touch a real Neon or Upstash instance, and a production mode that refuses to boot on a default JWT secret rather than appearing healthy.',
    techIcons: [
      { Icon: Python, label: 'Python' },
      { Icon: PostgresqlIcon, label: 'PostgreSQL' },
    ],
    tasks: [
      'Real persistence, real cookie-based JWT auth with refresh-token rotation and replay rejection, and real per-IP rate limits - the unhappy paths a read-only mock never teaches a client to handle',
      'Circuit breakers on both dependencies with opposite failure judgements: Postgres fails closed (503 + Retry-After), the Upstash limiter fails open to per-instance in-memory counters',
      'Sessions acquire their connection eagerly so the retry wrapper has a connect failure to catch - free, since it is the same BEGIN the first query would have issued',
      'Three-state health (ok / degraded / unhealthy) that distinguishes "Redis is down" from "Postgres is down", plus live circuit state, liveness, readiness and per-endpoint p95 latency',
      'Neon libpq connection strings normalised for asyncpg at the boundary, preserving sslmode=require as encrypt-not-verify and detecting the -pooler PgBouncer endpoint to disable statement caching',
      'Separate read and write rate-limit budgets in Upstash shared across instances, with every X-RateLimit-* header CORS-exposed because a header a browser cannot see does not exist',
      'CORS as the outermost middleware, so a 429 reaches the browser as a rate-limit response rather than an opaque "Failed to fetch" - order asserted in test_startup.py',
      'Production guardrails that refuse to boot on a default JWT secret or a SQLite database, because a misconfigured service that starts looks healthy right up until a token is forged',
      'First-boot seeding of ~9,000 rows behind a Postgres advisory lock so autoscaled replicas cannot double-seed; nightly auto-reset off by default for the same reason',
      'A reset script that prints its target and current row counts and requires the word "reset" to be typed before deleting anything',
      'A tutorial page at / with per-framework examples, whose endpoint tables are asserted against the real registered routes so the docs cannot drift',
      '213 tests with the environment pinned before app import, so a local .env can never leak into a run; Upstash exercised through httpx.MockTransport',
    ],
  },
  {
    id: 'game-rentals-platform',
    highlight: 'Peer-to-peer game rental marketplace with JWT auth, Redis caching over the RAWG catalogue, and automated late-fee calculation.',
    primaryTech: ['MongoDB', 'Express', 'Next.js', 'Redis'],
    title: 'Game Rentals Platform',
    shortDescription: 'Full-stack MERN application for peer-to-peer game rentals with JWT authentication, Redis caching, and automated late fee calculations.',
    category: 'Full Stack',
    technologies: ['MongoDB', 'Express', 'React', 'Next.js', 'Redis', 'TypeScript', 'JWT', 'RAWG API'],
    thumbnail: null, // You can add a thumbnail image path later
    featured: true,
    githubUrl: 'https://github.com/firepenguindisopanda', // Update with actual repo URL
    liveUrl: null, // Add live demo URL if available
    markdown: '/markdowns/game-rentals.md',
  },
  {
    id: 'ai-pitchdeck-generator',
    highlight: 'Generates a full pitch deck from a prompt - slide copy, speaker notes, jsonb-snapshot versioning and PDF export.',
    primaryTech: ['NestJS', 'Next.js', 'PostgreSQL', 'Socket.io'],
    title: 'AI Pitch Deck Generator',
    shortDescription: 'AI-driven system that auto-generates professional pitch decks with slide content, speaker notes, versioning, and export capabilities.',
    description: 'Full-stack, AI-assisted platform for generating professional pitch decks from simple prompts. Includes a NestJS backend, Next.js frontend, versioning, and PDF export.',
    category: 'Full Stack',
    technologies: ['NestJS', 'Next.js', 'TypeScript', 'PostgreSQL', 'NVIDIA NIM', 'Socket.io', 'PDFKit'],
    thumbnail: null,
    featured: true,
    githubUrl: 'https://github.com/firepenguindisopanda',
    liveUrl: null,
    markdown: '/markdowns/ai-pitchdeck-generator.md',
    techIcons: [
      { Icon: DataScience, label: 'AI' },
      { Icon: PostgresqlIcon, label: 'PostgreSQL' },
      { Icon: NodejsIcon, label: 'Node.js' }
    ],
    tasks: [
      'Generates slide content and speaker notes using Generative AI endpoints',
      'Supports deck versioning with PostgreSQL jsonb snapshot storage',
      'Real-time editing and reordering of slides via Socket.io and drag-and-drop UI',
      'PDF export with consistent styling and theming',
    ],
  },
  {
    id: 'rag-multi-agent-chatbot',
    highlight: 'Multi-agent software team over a FAISS vector store, handling document analysis and data questions through a LangGraph workflow.',
    primaryTech: ['LangGraph', 'FAISS', 'FastAPI', 'Streamlit'],
    title: 'RAG Multi-Agent Software Team Chatbot',
    shortDescription: 'Advanced AI-powered chatbot system with multi-agent software development team, document analysis, and data insights capabilities.',
    category: 'AI/ML',
    technologies: ['Full Stack', 'NVIDIA AI', 'FAISS', 'LangChain', 'LangGraph', 'Streamlit', 'FastAPI', 'Python'],
    thumbnail: null,
    featured: true,
    githubUrl: 'https://github.com/firepenguindisopanda/rag-chatbot-multi-agent-software-team',
    liveUrl: 'https://multi-agent-software-team.streamlit.app/',
    markdown: '/markdowns/rag-multi-agent-chatbot.md',
    techIcons: [ { Icon: DataScience, label: 'AI' }, { Icon: Python, label: 'Python' } ],
  },
  {
    id: 'uwi-dcit-chatbot',
    highlight: 'Department chatbot answering student questions from a FAISS index built on university course material.',
    primaryTech: ['FAISS', 'LangChain', 'Python', 'Google PALM'],
    title: 'UWI DCIT Chatbot',
    shortDescription: 'AI-powered chatbot using advanced vector database technology for intelligent responses.',
    category: 'AI/ML',
    technologies: ['FAISS', 'Python', 'Langchain', 'Google PALM', 'HuggingEmbeddings'],
    thumbnail: null,
    featured: true,
    githubUrl: 'https://github.com/firepenguindisopanda/experiment-uwi-dcit-chatbot',
    liveUrl: null,
    markdown: '/markdowns/uwi-dcit-chatbot.md',
    techIcons: [ { Icon: DataScience, label: 'AI' }, { Icon: Python, label: 'Python' } ],
  },
  {
    id: 'poke-dash',
    highlight: 'Pokedex platform pairing collection mechanics with an ML layer: a GridSearchCV-tuned Random Forest legendary classifier, KMeans segmentation and a synergy-scored team recommender.',
    primaryTech: ['Flask', 'scikit-learn', 'Socket.IO', 'PostgreSQL'],
    title: 'Poke-Dash: Pokemon Dashboard',
    shortDescription: 'Full-stack Pokemon management platform combining classic collection mechanics with modern machine learning analytics. Browse the complete Pokedex (801 Pokemon across 8 generations), hunt wild Pokemon in an interactive arena, test your knowledge with trivia quizzes, chat in real time, and explore advanced ML-powered insights.',
    category: 'Full Stack',
    technologies: ['Flask', 'Flask-SocketIO', 'Flask-JWT-Extended', 'Flask-SQLAlchemy', 'scikit-learn', 'pandas', 'NumPy', 'scipy', 'Chart.js', 'Materialize CSS', 'SQLite/PostgreSQL', 'Jinja', 'Render', 'joblib'],
    thumbnail: pokemonDashboardProPic,
    featured: true,
    githubUrl: 'https://github.com/firepenguindisopanda/Pokemon-Dashboard',
    liveUrl: 'https://pokemon-dashboard.onrender.com/',
    markdown: '/markdowns/poke-dash.md',
    evidence: 'The legendary classifier is tuned by GridSearchCV over StratifiedKFold with F1 scoring, not accuracy - on a class this imbalanced, accuracy would score a model that never predicts legendary at all.',
    techIcons: [ { Icon: Flask, label: 'Flask' }, { Icon: Python, label: 'Python' } ],
    tasks: [
      'JWT authentication with access/refresh token rotation, silent refresh, and DB-failure fallback to token claims',
      'Interactive arena with wild Pokemon encounters, HP-based damage (10-29 random), capture probability scaling with HP ratio and capture rate',
      '10-question Pokemon trivia quiz (stat, type, generation questions) awarding pokeballs for each correct answer',
      'Real-time chat via Flask-SocketIO with message persistence',
      'ML analytics dashboard with descriptive statistics, type distribution, generation distribution, and stat radar charts',
      'Random Forest legendary classifier with GridSearchCV hyperparameter tuning, StratifiedKFold (5-fold), and F1 scoring for imbalanced data',
      'Ensemble model comparison: Random Forest vs Gradient Boosting vs Logistic Regression vs soft Voting ensemble',
      'Base total regressor with R²/MSE evaluation and feature importance analysis',
      'KMeans clustering (PCA and t-SNE visualization) with cluster profiling (dominant types, generation distribution, legendary rate)',
      'Cosine-similarity Pokemon search and reverse search (find closest real Pokemon to custom stat profile)',
      'Team recommendation engine with 6 role-based slots, playstyle modes (offensive/defensive/speed/balanced), difficulty levels (beginner/intermediate/advanced), legendary capping (max 2), and synergy scoring',
      'Scipy build optimizer finding optimal stat distributions for maximum predicted base total',
      'Model caching system with data-hash versioning (MD5) and joblib persistence for fast reloads',
      'Type defense coverage analysis for team compositions',
    ],
  },
  {
    id: 'movie-app',
    highlight: 'Angular movie discovery app integrating TMDB and RAWG, with Supabase-backed comments and Cypress E2E tests shipped to Cloudflare Pages via GitHub Actions.',
    primaryTech: ['Angular 17', 'Material UI', 'Supabase', 'Cypress'],
    screenshot: movieShot,
    title: 'Movie App',
    shortDescription: 'Interactive movie discovery platform with user reviews and real-time API integration.',
    description: 'Angular-powered movie discovery platform featuring TMDB and RAWG API integration, Supabase-backed user comments with real-time persistence, and CI/CD deployment to Cloudflare Pages. Built with Angular 17+, Material UI, and Cypress E2E testing, the app lets users browse a curated collection of movies, view detailed metadata, and engage through a comment system with instant UI updates.',
    category: 'Frontend',
    technologies: ['AngularJS', 'Material UI', 'GitHub Actions', 'Netlify'],
    thumbnail: null,
    featured: true,
    githubUrl: 'https://github.com/firepenguindisopanda/DCIT-Movie-App',
    liveUrl: 'https://movie-game-viewer.pages.dev/',
    markdown: '/markdowns/movie-app.md',
    evidence: 'A Cypress end-to-end suite gates the GitHub Actions deploy to Cloudflare Pages, so a broken build never reaches the live URL.',
    techIcons: [ { Icon: Angular, label: 'AngularJS' }, { Icon: MaterialUi, label: 'Material UI' } ],
  },
  {
    id: 'recipe-app',
    highlight: 'Recipe manager with account handling and favourites, deployed on Heroku.',
    primaryTech: ['Flask', 'Jinja', 'SQLite', 'Heroku'],
    title: 'Recipe App',
    shortDescription: 'Full-stack web application for recipe management with user authentication and favorites.',
    category: 'Full Stack',
    technologies: ['Flask', 'Jinja', 'Heroku', 'SQLite'],
    thumbnail: null,
    featured: false,
    githubUrl: 'https://github.com/firepenguindisopanda/recipe-board',
    liveUrl: null,
    markdown: '/markdowns/recipe-app.md',
    techIcons: [ { Icon: Flask, label: 'Flask' } ],
  },
  {
    id: 'portfolio-website',
    highlight: 'This site. React and MUI with four switchable theme personalities, route-level code splitting and a Vitest suite in CI.',
    primaryTech: ['React', 'Material UI', 'Framer Motion', 'Firebase'],
    title: 'Portfolio Website',
    shortDescription: 'Personal portfolio showcasing projects and skills with modern design principles.',
    category: 'Frontend',
    technologies: ['React', 'Material UI', 'Firebase', 'Framer Motion'],
    thumbnail: null,
    featured: false,
    githubUrl: 'https://github.com/firepenguindisopanda/react-portfolio-website',
    liveUrl: 'https://nicksportfolio.tech/',
    markdown: '/markdowns/portfolio-website.md',
    evidence: 'A Vitest suite runs in CI on every push, and the four theme personalities share one set of structural tokens so they cannot drift into four different websites.',
    techIcons: [ { Icon: ReactIcon, label: 'React' }, { Icon: MaterialUi, label: 'Material UI' }, { Icon: FirebaseIcon, label: 'Firebase' } ],
  },
  {
    id: 'caribbean-asr-data-science',
    highlight: 'Speech-recognition pipeline for Caribbean English: Wav2Vec-BERT encoders fine-tuned with CTC, then KenLM beam rescoring through pyctcdecode.',
    primaryTech: ['PyTorch', 'Transformers', 'KenLM', 'torchaudio'],
    title: 'Caribbean ASR Data Science',
    shortDescription: 'ASR pipeline with Wav2Vec-BERT + CTC and KenLM tuned for Caribbean English.',
    description: 'Reproducible ASR pipeline using SSL encoders (Wav2Vec / Wav2Vec-BERT), CTC fine-tuning, KenLM rescoring and pyctcdecode. Includes notebooks (Starter, Full Pipeline) and scripts for training and decoding.',
    category: 'Data Science',
    technologies: ['PyTorch', 'Transformers', 'KenLM', 'pyctcdecode', 'torchaudio', 'Librosa'],
    thumbnail: caribbeanAsrThumbnail,
    featured: true,
    githubUrl: 'https://github.com/firepenguindisopanda/caribbean-asr',
    liveUrl: null,
    markdown: '/markdowns/caribbean-asr-data-science.md',
    techIcons: [ { Icon: DataScience, label: 'Data Science' }, { Icon: Python, label: 'Python' } ],
    tasks: [
      'Fine-tune SSL models with CTC for Caribbean English',
      'Train KenLM on normalized transcripts and add beam-rescoring with pyctcdecode',
      'Augment data using Speed/Pitch perturb and synthetic TTS for robust inference'
    ],
  },
  {
    id: 'wids-temp-forecasting',
    highlight: 'LightGBM forecaster for 14-day temperatures across 514 US locations - a 90% RMSE reduction against the NMME physics baseline.',
    primaryTech: ['LightGBM', 'Python', 'Pandas', 'scikit-learn'],
    title: 'WiDS 2023: Sub-Seasonal Temperature Forecasting',
    shortDescription: 'Predicting 14-day average temperatures across 514 US locations using climate and atmospheric data. LightGBM model achieves 90% RMSE reduction over NMME ensemble baseline.',
    description: 'End-to-end data science project forecasting sub-seasonal temperatures using the WiDS Datathon 2023 dataset. Features comprehensive EDA, feature engineering, and a LightGBM model trained on 375K+ samples with 150 selected features.',
    category: 'Data Science',
    technologies: ['LightGBM', 'Python', 'Pandas', 'Scikit-learn', 'Matplotlib', 'Seaborn', 'Jupyter'],
    thumbnail: widsThumbnail,
    featured: true,
    githubUrl: 'https://github.com/firepenguindisopanda/wids-2023-datathon',
    liveUrl: null,
    markdown: '/markdowns/wids-temp-forecasting.md',
    evidence: 'Scored against the NMME physics-based ensemble rather than a null model - a 90% RMSE reduction measured against the baseline meteorologists actually use.',
    techIcons: [
      { Icon: DataScience, label: 'Data Science' },
      { Icon: Python, label: 'Python' }
    ],
    tasks: [
      'Comprehensive EDA across 375K+ samples, 514 locations, 731 days',
      'Feature engineering with NMME ensemble statistics and interaction features',
      'LightGBM model with 90% RMSE reduction over physics-based baseline',
      'Mutual information feature selection reducing 246 features to top 150',
      'Interactive portfolio visualizations with pre-computed artifacts',
    ],
    portfolioData: '/portfolio_data/wids/',
  },

  {
    id: 'fraud-detection',
    highlight: 'A fraud model can be 99.8% accurate and catch zero fraud. Three datasets, leak-free pipelines, and a decision threshold derived from cost rather than the 0.5 default - which cuts losses 89%.',
    primaryTech: ['XGBoost', 'scikit-learn', 'statsmodels', 'Gradio'],
    title: 'Multi-Dataset Fraud Detection Pipeline',
    shortDescription: 'Leak-free fraud detection across three financial datasets, reported with average precision and bootstrap confidence intervals instead of accuracy. Cost-based threshold optimisation, formal hypothesis testing with effect sizes, odds-ratio interpretation, and a live interactive explorer.',
    description: 'Fraud detection across three financial datasets (Credit Card, Online Payment, Bank Account), rebuilt around the observation that a classifier predicting "never fraud" scores 99.83% accuracy while catching none of it. Average precision replaces accuracy and F1 as the headline metric, reported with bootstrap confidence intervals; the decision threshold is derived from a cost function (Z = 100xFN + 1xFP) rather than left at 0.5, cutting modelled losses by 89% on the Credit Card data. All preprocessing is fitted inside a pipeline on the training split only and refitted within every CV fold. Model differences are tested with paired methods (McNemar, DeLong, paired bootstrap) - which shows the top two models are not statistically distinguishable. Interpretation uses odds ratios with confidence intervals from a separate unpenalised fit, permutation importance on held-out data, and exact TreeSHAP attribution. Includes an honest post-mortem on the dataset that did not work, diagnosed via effect sizes: 24 of 25 features significant, none with a large effect. Every number in the README, report, site and demo is generated from one metrics artifact, with a test that fails the build on drift.',
    category: 'Data Science',
    technologies: ['Python', 'scikit-learn', 'XGBoost', 'statsmodels', 'SciPy', 'Pandas', 'NumPy', 'Gradio', 'pytest', 'uv', 'Cost Optimization', 'Hypothesis Testing', 'SHAP'],
    thumbnail: null,
    featured: true,
    githubUrl: 'https://github.com/firepenguindisopanda/fraud-analysis',
    liveUrl: null,
    markdown: '/markdowns/fraud-detection.md',
    evidence: 'The threshold is chosen by minimising Z = 100xFN + 1xFP rather than defaulting to 0.5. On the Credit Card data the optimum lands at 0.02, catching 88 of 98 frauds for 83 false alarms - $1,083 of modelled loss against $9,800 for doing nothing.',
    techIcons: [
      { Icon: DataScience, label: 'Data Science' },
      { Icon: Python, label: 'Python' }
    ],
    tasks: [
      'Rebuilt preprocessing as a leak-free sklearn/imblearn pipeline fitted on the training split only and refitted inside every CV fold, replacing transforms that previously ran on the full dataset before splitting',
      'Replaced accuracy and F1 with average precision as the headline metric, with bootstrap confidence intervals, lift over chance, TPR@5%FPR and precision at an analyst alert budget',
      'Derived the operating threshold from a cost function instead of the 0.5 default: 0.02 on Credit Card catches 90% of fraud for 83 false alarms, an 89% reduction in modelled loss',
      'Showed via a four-arm ablation that SMOTE plus class_weight="balanced" is a no-op rather than a double correction, since the weights evaluate to [1.0, 1.0] once SMOTE balances the classes',
      'Added formal hypothesis testing with multiplicity correction: Mann-Whitney and KS with Cliff\'s delta, chi-squared with Cramer\'s V and Wilson intervals, McNemar, DeLong and paired bootstrap for model comparison',
      'Established that XGBoost and Random Forest are not statistically distinguishable on Credit Card (delta AP -0.017, CI spanning zero), so the "best model" claim is hedged rather than overstated',
      'Interpreted the linear model as odds ratios with confidence intervals from a separate unpenalised statsmodels fit, in both per-standard-deviation and original units, with VIF',
      'Diagnosed the failing Bank Account dataset via effect sizes (24 of 25 features significant, none large) and found a real data bug: negative missing-value sentinels in 71.9% of one column that also silently disabled the log transform',
      'Shipped an interactive Gradio explorer running on ~30MB of artifacts and none of the 819MB of raw data, with per-prediction SHAP attribution and no shap/numba runtime dependency',
      'Made numbers undriftable: one metrics artifact feeds the README, HTML report, this site and the demo, with 75 tests including a documentation-drift check and 1e-9 inference parity',
    ],
    portfolioData: '/portfolio_data/fraud_detection/',
  },
  {
    id: 'starbucks-offer-analysis',
    highlight: 'Offer-targeting study across 306K events: four K-Means segments, an XGBoost classifier at AUC-ROC 0.994, and a rule-based recommender delivering +7.9% lift.',
    primaryTech: ['XGBoost', 'scikit-learn', 'SHAP', 'Plotly'],
    title: 'Starbucks Offer Analysis & Recommendation',
    shortDescription: 'End-to-end data science project optimizing Starbucks offer targeting through customer segmentation (K-Means, k=4), predictive modeling (XGBoost AUC-ROC=0.994), and a rule-based recommendation system achieving +7.9% lift with rigorous causal inference and statistical validation.',
    description: 'Comprehensive analysis of 17K customers, 10 offers, and 306K+ events from simulated Starbucks Rewards app data. Features EDA with hypothesis tests (Mann-Whitney U, Chi-squared, Cohen\'s d), feature engineering (55 customer + 75 interaction features), K-Means clustering (4 segments with stability analysis ARI=0.85), XGBoost classifier (AUC-ROC=0.994, 5-fold CV, threshold optimization), causal ATE analysis with bootstrap CIs and propensity score matching, and a rule-based recommendation system with +7.9% lift and A/B test simulation framework.',
    category: 'Data Science',
    technologies: ['Python', 'XGBoost', 'scikit-learn', 'Pandas', 'NumPy', 'Matplotlib', 'Seaborn', 'Plotly', 'SHAP', 'SciPy'],
    thumbnail: null,
    featured: true,
    githubUrl: 'https://github.com/firepenguindisopanda/starbuck-analysis-project',
    liveUrl: null,
    markdown: '/markdowns/starbucks-offer-analysis.md',
    evidence: 'Segments are only reported after a stability check (ARI=0.85), and the causal claims carry bootstrap confidence intervals and propensity score matching rather than raw group differences.',
    techIcons: [
      { Icon: DataScience, label: 'Data Science' },
      { Icon: Python, label: 'Python' }
    ],
    tasks: [
      'Built end-to-end pipeline processing 306K+ events across 17K customers and 10 offers',
      'Engineered 55 customer features + 75 interaction features with RFM, time-decay, and CLV proxy metrics',
      'Identified 4 customer segments via K-Means with stability validation (ARI=0.85) and business metric profiling',
      'Trained XGBoost classifier achieving AUC-ROC=0.994 with 5-fold cross-validation and threshold optimization (F1=0.612 at t=0.42)',
      'Performed causal ATE analysis with bootstrap CIs, propensity score matching, and heterogeneous treatment effects by segment',
      'Built A/B test simulation framework (power=0.80, n=3,277 per group) and recommendation system achieving +7.9% lift',
      'Added SHAP model explainability, Brier score calibration (0.133), and 25+ publication-quality visualizations',
    ],
    portfolioData: '/portfolio_data/starbucks/',
  },
  {
    id: 'uwi-scraper',
    highlight: 'Cache-aware async crawler cataloguing 20+ UWI domains - respects robots.txt, uses ETag conditional requests, and serves results through PostgreSQL full-text search.',
    primaryTech: ['FastAPI', 'PostgreSQL', 'HTMX', 'Redis'],
    screenshot: uwiScraperShot,
    title: 'UWI Scraper: Cache-Aware Web Crawler & Link Catalog',
    shortDescription: 'A cache-aware web crawler that systematically crawls UWI web domains, extracts page metadata, classifies pages by campus/category/type, and presents everything in a searchable, filterable web directory with inline editing and scheduled job management.',
    category: 'Full Stack',
    technologies: ['Python', 'FastAPI', 'PostgreSQL', 'HTMX', 'Alpine.js', 'Redis', 'BeautifulSoup', 'SQLModel', 'Alembic', 'APScheduler', 'Jinja2'],
    thumbnail: null,
    featured: true,
    topPick: true,
    githubUrl: 'https://github.com/firepenguindisopanda',
    liveUrl: 'https://uwi-scraper.fastapicloud.dev/',
    markdown: '/markdowns/uwi-scraper.md',
    evidence: 'Polite by construction: robots.txt honoured including Crawl-Delay, ETag and If-Modified-Since conditional requests, and a semaphore capping concurrency per domain - a crawler that behaves well when nobody is watching.',
    techIcons: [
      { Icon: Python, label: 'Python' },
      { Icon: PostgresqlIcon, label: 'PostgreSQL' },
    ],
    tasks: [
      'Polite, cache-aware async crawler respecting robots.txt with ETag/If-Modified-Since conditional requests and configurable rate limits',
      'Rich metadata extraction: page titles, descriptions, body text, links, and external resources',
      'Rule-based URL classifier detecting 7 campuses, 10 categories, and link types across 20+ UWI domains',
      'Browsable link catalog with paginated grid view, faceted filtering by campus/category/domain/status',
      'PostgreSQL full-text search with tsvector indexing and ts_rank ordering for relevance-ranked results',
      'HTMX-powered inline editing and Alpine.js batch editing for classification fields without page reloads',
      'Admin dashboard with aggregate metrics, hidden login URL, bcrypt auth, and Redis-backed sessions',
      'Scheduled crawl jobs via APScheduler with cron-based scheduling and background health-check runs',
    ],
  },
  {
    id: 'bi-automatic-reporting',
    highlight: 'Submit a URL, get a researched business analysis as PDF and DOCX. Every stage is scored twice - a deterministic contract plus an LLM critic - and a 7-fixture eval harness gates prompt changes.',
    primaryTech: ['FastAPI', 'React 19', 'LangChain', 'NVIDIA NIM'],
    // Captured with the model configuration panel open, so the eight pipeline
    // stages and their per-stage model routing are visible rather than just an
    // empty submission form.
    screenshot: biReportingShot,
    title: 'Automated BI Reporting System',
    shortDescription: 'Automated business intelligence platform that scrapes a target site, runs a nine-stage AI analysis pipeline, and emails PDF + DOCX reports - with per-stage quality scoring, SSE progress, and an offline eval harness that fails the build on regression.',
    description: 'Full-stack BI platform (FastAPI + React 19) where the interesting half is the machinery built to know whether the LLM output is any good. Nine canonical pipeline stages each record duration, model, prompt version, attempts and token usage to a job_stages table, and carry two independent quality scores: a deterministic completeness score against a per-stage contract, and an optional LLM critic confidence score that triggers a targeted re-run below threshold. The agent runtime replaces six duplicated agent classes with one spec plus a runner whose retry ladder distinguishes parse failures (repair turn feeding back the raw output and error) from transport failures (backoff with jitter). Progress streams over SSE with a polling fallback serving the identical payload, so a mid-run refresh reconstructs the full timeline. An offline eval harness runs seven saved fixtures - including a government site as a hallucination canary - and fails on regression. Hardened with an SSRF guard that re-validates every redirect, Upstash-backed rate limiting, and a five-provider email registry that raises at startup rather than silently dropping mail.',
    category: 'Full Stack',
    technologies: ['Python 3.13', 'FastAPI', 'React 19', 'TypeScript', 'Vite', 'Tailwind CSS v4', 'LangChain', 'NVIDIA NIM', 'PostgreSQL', 'SQLite', 'SSE', 'Docker', 'nginx', 'pytest', 'ruff', 'mypy'],
    thumbnail: null,
    featured: true,
    topPick: true,
    githubUrl: 'https://github.com/firepenguindisopanda/bi-automatic-reporting',
    liveUrl: null,
    markdown: '/markdowns/bi-automatic-reporting.md',
    evidence: 'Every stage carries two independent quality scores - a deterministic completeness contract and an LLM critic - and a seven-fixture eval harness, one of them a government site included as a hallucination canary, fails the build on regression.',
    techIcons: [
      { Icon: Python, label: 'Python' },
      { Icon: ReactIcon, label: 'React' },
    ],
    tasks: [
      'Nine-stage analysis pipeline (scrape, profile, market, competitive, SWOT, marketing, report, render, deliver) defined once server-side and served to the frontend, so stage lists cannot drift',
      'Two quality scores per stage: deterministic completeness against a StageContract (required fields, list minimums, grounding, placeholder-leak detection) plus an LLM critic confidence score that re-runs the stage once below threshold',
      'Agent runtime with a retry ladder that distinguishes failure modes - a repair turn feeding back raw output and the parse error, then a reduced schema hint, with transport errors retried separately on backoff with jitter',
      'Job state model with a load-bearing "partial" status (report produced, delivery failed) and "interrupted" set by startup recovery, so a killed container never leaves a job stuck on running',
      'SSE progress with 15s heartbeat plus a polling fallback serving the identical payload; status returns the full stage array so a refresh restores exact timings and scores',
      'ETA from rolling median duration per (stage, model) over the last 20 runs, suppressed until 3 samples exist',
      'Offline eval harness over 7 saved HTML fixtures chosen for distinct failure modes (including a non-business site as a hallucination canary), scored completeness minus 15x failed assertions, with a --check regression gate',
      'SSRF guard refusing loopback, private, link-local, reserved and CGNAT ranges and re-validating after every redirect; Upstash-backed rate limiting at 10 submits/60s per caller',
      'Five-provider email registry (SMTP with auth+TLS, SendGrid, Resend, console, file) that raises at startup on misconfiguration instead of silently falling back to unauthenticated localhost:25',
      'Editorial design direction enforced in CI by a script that rejects gradients, oversized radii and raw hex in place of tokens',
      '125 tests across 14 files, deployed via Docker Compose on a Raspberry Pi behind nginx',
    ],
  },
  {
    id: 'handbooks-parser',
    highlight: '2,195 courses and 141 degree programmes extracted from 8 differently-formatted university handbook PDFs into a normalized SQLite database - with an audit script that re-reads the PDFs independently to prove it.',
    primaryTech: ['Python', 'pdfplumber', 'SQLite', 'pandas'],
    title: 'UWI Handbook Parser',
    shortDescription: 'A document-engineering pipeline that turns 45MB of university faculty handbook PDFs into a queryable SQLite database of courses, degree programmes, and degree-to-course mappings - plus a separate verification tool that audits extraction accuracy against the source.',
    description: 'Eight UWI faculty handbooks, laid out to eight different conventions, converted into 2,195 courses, 141 degree programmes and 3,867 degree-to-course mappings. The data exists only as prose inside PDFs, so questions like "which degrees require this course?" are answerable only by hand - this makes them a SQL query. Pages are triaged before extraction using statistical detection (a page is a staff listing if >30% of its lines match academic-rank markers, not merely because it mentions a lecturer), faculty and department context is carried forward across pages, and multiple degree-extraction strategies run in order with dedicated extractors for the two faculties the generic approach could not handle. Courses are deduplicated by code preferring the richer description. A separate ~1,000-line verification script re-reads each source PDF independently of the parser and reports miss-rates, field completeness, unresolved degree references and section-boundary mismatches - built separately on purpose, since an audit sharing the extraction code would only agree with itself.',
    category: 'Full Stack',
    technologies: ['Python 3.13', 'pdfplumber', 'pypdf', 'pandas', 'openpyxl', 'SQLite', 'uv'],
    thumbnail: null,
    featured: true,
    githubUrl: null,
    liveUrl: null,
    markdown: '/markdowns/handbooks-parser.md',
    evidence: 'A separate ~1,000-line audit script re-reads every source PDF independently of the parser, because an audit sharing the extraction code would only ever agree with itself.',
    techIcons: [
      { Icon: Python, label: 'Python' },
    ],
    tasks: [
      'Extracted 2,195 courses, 141 degree programmes and 3,867 degree-to-course mappings from 8 faculty handbook PDFs (~45MB)',
      'Statistical page triage discarding staff listings, plagiarism forms, regulations and blank spacers before they pollute extraction',
      'Faculty detection from page headers with filename fallback, plus department context carried forward and a course-prefix-to-department lookup',
      'Multiple degree-extraction strategies tried in order (numbered sections, Roman-numeral sections, freeform headings) with dedicated extractors for the Humanities and Law layouts',
      'Course deduplication by code that prefers the entry with the richer description, so a bare title never overwrites a full syllabus',
      'Separate ~1,000-line audit script re-reading source PDFs independently: code miss-rate, field completeness, unresolved degree references, section-boundary checks, and sample side-by-side comparisons',
      'Normalized SQLite build with UNIQUE constraints, an ON DELETE CASCADE foreign key, and 11 indexes on the filtered columns',
      'Excel intermediates (courses, degrees, degree-courses) plus a stats JSON for per-faculty and per-department totals',
    ],
  },
  {
    id: 'pdf-tools',
    highlight: 'Offline Tauri desktop app - Rust core, Angular 20 UI - that merges and splits PDFs by rebuilding the object graph, and cross-checks folders of PDF/DOCX claim forms for double-booked time slots.',
    primaryTech: ['Rust', 'Tauri v2', 'Angular 20', 'lopdf'],
    title: 'PDF Tools: Offline Desktop PDF Utility',
    shortDescription: 'Cross-platform Tauri v2 desktop app with a Rust PDF engine and an Angular 20 UI. Merges and splits PDFs entirely offline, and audits folders of PDF/DOCX claim forms for overlapping time-slot bookings no one would catch by hand.',
    description: 'A desktop-first PDF utility, built as a native app precisely because the documents it handles - claim forms, invoices, internal reports - should never leave the machine. The Rust core uses lopdf to manipulate the PDF object graph directly: merging renumbers each source document\'s objects, copies everything except Catalog/Pages/Page, then rebuilds a fresh page tree with rewritten Parent pointers and a new Catalog root; splitting does the reverse. The third tool is a conflict detector: it extracts (date, course code, time range) entries from every PDF and DOCX in a folder and reports overlaps. Getting there required a fragment-reassembly pre-pass, since lopdf text extraction returns kerning-split fragments that no regex can match, and two parsing strategies tried in order - a strict inline pattern first, falling back to a line-state-machine only when it finds nothing, so the fuzzy path cannot invent entries on documents the strict one already handled. DOCX parsing needs no Word library: unzip, stream word/document.xml with quick-xml, and emit a newline per paragraph close.',
    category: 'Desktop Tools',
    technologies: ['Rust', 'Tauri v2', 'Angular 20', 'TypeScript', 'lopdf', 'quick-xml', 'serde', 'regex'],
    thumbnail: null,
    featured: true,
    githubUrl: null,
    liveUrl: null,
    markdown: '/markdowns/pdf-tools.md',
    evidence: 'Two parsing strategies run in a fixed order - strict inline regex first, a line state machine only when it finds nothing - so the fuzzy path can never invent entries on a document the strict one already handled.',
    techIcons: [
      { Icon: Angular, label: 'Angular' },
      { Icon: Windows, label: 'Desktop' },
    ],
    tasks: [
      'Fully offline desktop app - no upload, no server, no account - shipped as a single native binary per platform via Tauri v2',
      'PDF merge that rebuilds the object graph: per-document object renumbering, a fresh Pages tree with rewritten Parent pointers, and a new Catalog root',
      'Three split methods (every N pages, explicit ranges, break-at-pages) with ranges clamped to the real page count and break points sorted, deduped and bounded',
      'Claim form conflict detector reading both PDF and DOCX, extracting (date, course code, time range) entries and reporting overlapping bookings across files',
      'Fragment-reassembly pre-pass that glues kerning-split text fragments back into lines, turning unparseable lopdf output into parseable input',
      'Two parsing strategies tried in order - strict inline regex first, line state machine as fallback - so the fuzzy path cannot produce false entries',
      'Half-open interval overlap detection over minutes-past-midnight with explicit 12am/12pm handling, grouping N-way clashes into one conflict rather than pairwise duplicates',
      'Document logic kept free of Tauri dependencies so it is testable as plain Rust, with one AppError enum serializing straight to user-facing strings',
      'Angular standalone components with lazy routes, modern @if/@for control flow, and Tauri capabilities scoped to dialog open/save only',
      'Degraded-input handling throughout: missing or empty PDFs become warnings and are skipped, unparseable files become per-file errors instead of blanking the report',
    ],
  },
];
